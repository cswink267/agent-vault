---
name: agent-vault
description: Store and retrieve shared credentials via agent-vault. Use when the user provides passwords, API keys, tokens, or logins; when a task needs credentials; or when integrating with services that require secrets.
---

# Agent Vault (generic agents)

This stub exists for hosts that look under `skills/generic/`. **Use the universal skill:**

→ **[`../SKILL.md`](../SKILL.md)**

→ Full guide: [`../../docs/agents.md`](../../docs/agents.md)

→ MCP example: [`../mcp.stdio.json.example`](../mcp.stdio.json.example)

Works with Windsurf, Continue, Cline, Roo, Open WebUI tools, custom MCP hosts, shell-only agents, and anything that can call HTTP.
